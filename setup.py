#!/usr/bin/env python

from distutils.core import setup

setup(name='dogeared-api',
      version='0.1',
      description='Python bindings for the dogeared-www API',
      author='Aaron Straup Cope',
      url='https://github.com/straup/py-dogeared-api',
      requires=[],
      packages=['dogeared'],
      download_url='https://github.com/straup/py-dogeared-api/releases/tag/v0.1',
      license='BSD')
